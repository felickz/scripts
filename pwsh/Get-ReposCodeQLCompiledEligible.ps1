#using the gh cli that will list all repos (for my octodemo org)  for the CodeQL compiled languages (default manual enabled) and then query each to see if there are any code scanning analyses or not (404).

gh api search/repositories -X GET -f q="org:octodemo language:csharp,swift,c,cpp,java,kotlin" --jq '.items | map(.full_name) | .[]' --paginate | ForEach-Object { $(Invoke-RestMethod -Uri https://api.github.com/repos/$_/code-scanning/analyses -Headers @{ Authorization="Bearer $(gh auth token)"} -SkipHttpErrorCheck ).message -eq "no analysis found" ? "$_ - No Analysis" : "$_ - Found Analysis"}
